# Mappers

Pure transformation helpers between raw data shapes and domain-ready shapes.

Examples:
- raw course record -> catalog course model
- notification record -> sorted message item
- lesson schedule item -> lesson detail projection
