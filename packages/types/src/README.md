# Types Source Layout

Each domain should define its own contracts in a separate folder.

Example:
- `course/`
- `lesson/`
- `assessment/`
- `message/`

Keep domain boundaries consistent with backend modules and admin features.
