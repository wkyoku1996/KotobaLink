# Config

Mini Program configuration constants live here.

Use this directory for:
- local storage keys
- demo mode keys
- default local state
- feature flags that are specific to the Mini Program app

Editing rule:
- If a value changes how the app is configured, it belongs here.
- If a value is business fixture data for pages, it belongs in `mock/`.
