# Legacy Pages

This directory contains WeChat template pages kept only for reference.

Current rules:
- These pages are not registered in `app.json`.
- New product work must not depend on them.
- If a template is reused, move the useful code into the current domain structure first.
